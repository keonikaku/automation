# Runtime dependencies. Pinned so a clean clone resolves to the versions this
# suite was proven against. After installing, fetch the browser binaries:
#
#     pip install -r requirements.txt
#     playwright install chromium
#
playwright==1.58.0
pytest==9.0.3
pytest-html==4.2.0

# Native iOS (tests/native). Pure Python, so it installs everywhere; the test
# it supports is deselected by default and never runs in CI. It is pinned here
# rather than left optional so that `pytest --collect-only` can import every
# test module on any platform — collection integrity is a CI gate.
Appium-Python-Client==5.3.1
